(function($){
    $.fn.cnpj = function(callback){
         
        $(this).on('keyup',function(){
           var val = $(this).val().replace(/\D+/g,'');           
           if(pureVal.length === 12){
               
           }           
        });
        
        var firstDigit = function(val){
            
        };
        
        var secondDigit = function(val){
            
        };
        
        var isValid = function(val){
            
        };
    };
})(jQuery);
